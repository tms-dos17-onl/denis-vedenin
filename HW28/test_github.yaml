name: Test-GitHubActions

on: 
  push:
    branches:
      - main

jobs:
  Clone:
    runs-on: ubuntu-latest
    steps:
    - name: Echo
      run: echo "hello world!" > hello.txt
    - name: Ls
      run: ls -la


  Build:
    runs-on: ubuntu-latest
    steps:
    - name: Ls
      run: ls -la